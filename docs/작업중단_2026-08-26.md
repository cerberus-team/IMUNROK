# 작업 중단 지점 — 2026-08-26

> 이어서 할 때 이 문서부터 읽을 것.

---

## A. UI 통합 (진행 중 · 미완)

견우 대화창을 서천에 들이는 작업. **2번 경로**(팀원 UI + 우리 것 얹기)로 진행 중.

### 완료

| 파일 | 상태 |
|---|---|
| `Assets/IMUNROK_UI/` (40개) + 폰트 4개 | 임포트 완료 · 충돌 0건 |
| `SeocheonDialogueBackend.cs` | ✅ 컴파일됨. `IDialogueBackend` 구현, Gemini 834줄 그대로 씀 |
| `SeocheonDialogueExtras.cs` | ✅ 컴파일됨. 선택지·워드픽·「증거제시」 감추기 |
| `SeocheonDialogueBar.cs` (1462줄) | ✅ 컴파일됨. 견우 `DialogueUI` 사본 + `SeocheonBarStyle` |
| `IMUNROK.Seocheon.asmdef` | `IMUNROK.Ui` 참조 추가됨 |

### 다음에 할 일 (여기서 멈춤)

1. `SeocheonDialogueBar.StyleOf()` 에 선택지 값 넣기
   ```
                    PC     VR
   askH             62     76
   askGap           12     14
   lineToAsk        28     34
   판 높이     461→625  535→713
   ```
2. `BuildBottom()` 에 선택지 2×2 그리기 (대사와 입력줄 사이)
3. `SeocheonDialogue.cs` 배선을 새 경로로 교체
4. 정리 — 옛 `SeocheonDialogueCanvas.prefab` · `SeocheonDialogueUI.cs` · `SeocheonDialogueVrHost.cs` 처분

### 미결 질문
- 선택지를 **2×2** 로 할지 **1×4 세로** 로 할지 (세로면 PC 461→790)
- 증거 제시는 서천에 **없음** 확정 (`Present()` 는 늘 false)

### 되돌리기
`Assets/_Project/Seocheon/Prefabs/UI/_bk/*_PREVRUI_20260826_210833.*` 에 변경 전 프리팹 5개 + `SeocheonDialogueUI.cs` 원본 보관.

---

## B. 지침서 대비 미처리 (우선순위 높음)

`이문록_통합작업_지침서.md` 기준. 상세는 아래 「C」.

```
[ ] .gitignore 두 곳 수정   ← 이것부터
[ ] git add Assets/_Project Assets/Models Assets/_Sindang Assets/_Import
[ ] commit + push
[ ] 비스토어 4폴더 ZIP 압축 (unitypackage 아님)
[ ] 에셋 스토어 캐시 USB 복사 (임포트된 21개만)
[ ] 팀에 AllSkyFree 누락 알리기
```

---

## C. 지침서 점검 결과 요약

- `.meta` 누락 **400개** — 목록: `docs/meta_누락목록.txt`
- `.gitignore` 102줄 `/[Aa]ssets/Models/` → 자체 제작 모델 차단
- `.gitignore` 172~173줄 `/[Aa]ssets/**/*.asset(.meta)` → **지형 데이터·궁서체 TMP 에셋 차단**
- 지침서 임포트 목록에 `AllSkyFree` 없음 → 우리 에디터 스크립트가 하드코딩으로 참조 중

---

## D. 참고 문서

```
docs/에셋출처조사.md        에셋 출처·삭제 기록
docs/meta_누락목록.txt      .meta 누락 399줄
_TeamHandoff/README.md      설정 파일 인수인계 (ProjectSettings·URP)
```
