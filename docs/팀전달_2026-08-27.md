# 합치기 전 전달 사항 — 제3사건(견우) → 팀

`feature/case-gyeonu` 를 합치기 전에 읽어 주세요. **git 으로 안 가는 것**과
**내 개인 도구라 빼도 되는 것**을 적어 둡니다.

---

## 1. 패키지 — 하나는 필수, 둘은 개인 도구

`Packages/manifest.json` 에 네 줄이 늘었습니다.

| 패키지 | 성격 | 빼도 되나 |
|---|---|---|
| `com.whinarn.unitymeshsimplifier` (GitHub) | **필수** | ❌ 빼면 컴파일 실패 |
| `com.unity.xr.oculus` 4.5.5 | Quest 3 Link 용 | 판단은 공통 담당자 |
| `com.coplaydev.unity-mcp` (GitHub) | **개인 도구** | ✅ 빼도 됨 |
| `com.unity.recorder` 5.1.7 | **개인 도구** | ✅ 빼도 됨 |

- `UnityMeshSimplifier` 는 `Assets/_Project/Gyeonu/Editor/WoljeonggyoReducer.cs` 가 쓰고
  `IMUNROK.Gyeonu.Editor.asmdef` 가 어셈블리를 참조합니다. 없으면 **견우 에디터 어셈블리가
  통째로 컴파일 실패**합니다. 지우지 마세요.
- `unity-mcp` · `recorder` 는 제 작업용입니다. 거슬리면 Package Manager 에서 제거하세요 —
  코드가 참조하지 않으므로 아무것도 깨지지 않습니다.

## 2. ProjectSettings — 씬 등록만 올렸습니다

`ProjectSettings/EditorBuildSettings.asset` 에 견우 씬 6개를 등록했습니다.
(`Gyeonu_EunhaDam` · `Gyeonu_Gwana` · `Gyeonu_GwanaOffice` · `Gyeonu_Observatory` ·
`Gyeonu_GyeonuVillage` · `Gyeonu_SeonaHouse`)

이게 없으면 `SceneManager.LoadScene("Gyeonu_EunhaDam")` 이 **런타임에 실패**합니다.
순수 추가라 머지 충돌이 나도 양쪽 항목을 다 남기면 됩니다.

**같이 딸려 왔던 XR 설정 두 줄은 손으로 빼고 커밋했습니다** — 제 XR 설정이 팀에
퍼지면 안 되기 때문입니다.
```
Unity.XR.Oculus.Settings
com.unity.xr.management.loader_settings
```
그 밖의 `ProjectSettings/` 변경은 전부 되돌렸습니다 (`QualitySettings` 의 `lodBias 2→4` 는
제 로컬 확인용이라 원복).

## 3. XR 설정 — 공통 담당자에게 파일로 넘깁니다

커밋하지 않고 gitignore 로 막았습니다. 공통 담당자가 판단해 커밋해 주세요.

```
Assets/XR/                                     (7개, 3KB)
Assets/Resources/OVROverlayCanvasSettings.asset (+ .meta)
```
`Assets/Resources/OculusRuntimeSettings.asset` 의 `allowVisibilityMesh 0→1` 도
제 로컬에만 있습니다 — 필요하면 말씀해 주세요.

---

## 4. USB 로 넘겨야 하는 것 (gitignore 라 git 으로 안 감)

### ① 반드시 받아야 함 — 스토어에서 못 구하거나 GUID 가 어긋남

| 폴더 | 크기 | 왜 |
|---|---:|---|
| `Assets/woljeonggyo/` | 1,059 MB | Sketchfab. 각자 내려받으면 GUID 가 달라져 월정교가 사라짐 |
| `Assets/Joseon Library/` | 413 MB | 서고 기물 12종 |
| `Assets/Models/` | 217 MB | 관아 외삼문 게이트 세트 |
| `Assets/Unity_Final_Characters_Textured/` | 115 MB | NPC 11인. 씬 6개가 GUID 를 물고 있음 |
| `Assets/Donheon_v1/` | 104 MB | 팀원 제작 동헌 |
| `Assets/_Project/_Common/Art/Fonts/*.ttf` | 62 MB | 조선 궁서체 4종. LFS 사용량 0 방침이라 제외 |
| `Assets/Shaders/Uber Stylized Water/` | 57 MB | GitHub(MatrixRex v1.1.2). 물 재질이 GUID 를 물고 있음 |
| `Assets/_Import/` | 4 MB | 관아 나졸 |
| `Assets/GwanaHandoff/` | 0.2 MB | 관아 전달본 |
| **Meshy 유물 텍스처 52개** | **329 MB** | `Art/Materials/Artifacts/*` · `Art/Models/Items/*` — **FBX 는 커밋돼 있는데 텍스처만 빠져 있음.** 안 받으면 유물이 무텍스처로 뜸 |

> ⚠️ 위 폴더들은 `.meta` 만 커밋돼 있습니다. **폴더째 덮어쓰지 말고 파일만 채우세요** —
> `.meta` 를 덮으면 GUID 가 바뀌어 씬 참조가 끊깁니다.

### ② 각자 에셋 스토어에서 임포트 (넘길 필요 없음)

`Naganeupseong` 812MB · `Soswaewon` 958MB · `SeyeonjeongPavilion` 3,393MB ·
`KimMyeonggwanHouse` 789MB · `Waldemarst` 2,138MB · `BK_AlchemistHouse` 1,573MB ·
`UnhyeongungCollect` 2,030MB · `KTinteractiveProp` 234MB · `ALP_Assets` 87MB ·
`HwaseongHaenggung` 89MB · `Fantasy Skybox FREE` 91MB
그리고 `Window ▸ TextMeshPro ▸ Import TMP Essential Resources`

### ③ 메뉴로 다시 만들면 되는 것 (넘길 필요 없음)

| 대상 | 만드는 법 |
|---|---|
| `Art/Lighting/EnvCube_*.asset` (개당 256MB) | `Tools ▸ 이문록 ▸ 성하리 하늘 설정` |
| `Art/Fonts/Resources/` 구운 TMP 에셋 (9MB) | `Tools ▸ 이문록 ▸ 글꼴 ▸ TMP 폰트 에셋 굽기` |
| 견우 텍스처 38개 (26MB, 장부·비밀지도·관측실 등) | 각 굽기 메뉴 |

---

## 5. 저장소 상태 (2026-08-27 점검)

- Git LFS 사용량 **0** — `git lfs ls-files --all` 비었음. 방침 유지 중
- 역사에 외부 에셋 폴더가 커밋된 적 **없음**
- 역사·현재 추적 파일에 **API 키 없음** (`gemini_api_key.txt` 는 gitignore)
- 씬 7개가 참조하는 GUID 2,970개 전이 추적 — **비-스토어 `.meta` 누락 없음**
- ⚠️ 다만 Meshy 유물 **FBX 13개 약 200 MB 가 일반 blob 으로 커밋돼 있습니다.**
  `.gitattributes` 에 LFS 예외가 걸려 있어 의도된 것으로 보이나, 클론이 그만큼 무겁습니다
